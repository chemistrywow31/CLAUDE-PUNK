# CLAUDE PUNK - Electron App

## 🎮 使用方式

### 1. 啟動服務

首先,啟動後端和前端服務:

```bash
cd /path/to/CLAUDE-PUNK
./start.sh
```

這會啟動:
- **後端** (預設 port 3000): http://127.0.0.1:3000
- **前端** (預設 port 5173): http://localhost:5173

### 2. 啟動 Electron App

```bash
npm run start
```

Electron app 會:
1. 檢查服務是否運行
2. 如果未運行,顯示提示訊息
3. 如果已運行,開啟遊戲視窗

## ⚙️ 設定 Port

### 方法 1: 手動編輯設定檔

設定檔位置:
```
~/Library/Application Support/CLAUDE PUNK/config.json
```

編輯 JSON:
```json
{
  "backend": {
    "port": 3000,  // 改成你想要的 port
    "autoRunClaude": true,
    "claudePath": "/Users/your-name/.local/bin/claude"
  },
  "frontend": {
    "port": 5173  // 改成你想要的 port
  },
  "app": {
    "openBrowserOnStart": false
  }
}
```

### 方法 2: 透過 App 選單

1. 啟動 Electron app
2. 選單列 > **CLAUDE PUNK** > **Preferences** (⌘,)
3. 點擊 **"Open Config File"**
4. 編輯設定檔
5. 儲存並關閉
6. **重要**: 重啟 `./start.sh` 讓新 port 生效

```bash
# 終止舊的服務
./stop.sh

# 用新 port 啟動
PORT=4000 VITE_PORT=6000 ./start.sh
```

## 📋 設定說明

| 設定項目 | 說明 | 預設值 |
|---------|------|--------|
| `backend.port` | 後端 API port | 3000 |
| `frontend.port` | 前端 Vite dev server port | 5173 |
| `backend.claudePath` | Claude CLI 執行檔路徑 | `~/.local/bin/claude` |
| `backend.autoRunClaude` | 開啟專案時自動執行 Claude | true |
| `app.openBrowserOnStart` | 啟動時開啟外部瀏覽器 | false |

## 🔧 開發流程

### 正常開發 (不使用 Electron)

```bash
./start.sh
# 在瀏覽器開啟 http://localhost:5173
```

### 使用 Electron 視窗

```bash
# Terminal 1: 啟動服務
./start.sh

# Terminal 2: 啟動 Electron
npm run start
```

## 📦 打包 (未來功能)

```bash
# 打包成 .dmg (TODO: Phase 5-6)
npm run build
```

## ❓ 常見問題

### Q: Electron 啟動時顯示 "Services Not Running"

**A:** 先執行 `./start.sh` 啟動後端和前端服務。

### Q: 修改 port 後無法連接

**A:**
1. 確認設定檔已更新
2. **重啟 `./start.sh`**（重要！）
3. 重啟 Electron app

### Q: 如何查看日誌?

**A:**
- Electron 日誌: `~/Library/Logs/CLAUDE PUNK/main.log`
- 後端日誌: `/tmp/claudepunk-backend.log`
- 前端日誌: `/tmp/claudepunk-frontend.log`

### Q: 關閉 Electron 後服務還在運行?

**A:** 是的! Electron 不管理服務生命週期。使用 `./stop.sh` 關閉服務:
```bash
./stop.sh
```

## 🏗️ 架構說明

```
┌─────────────────────────────────────┐
│     Electron App (視窗 + 設定)       │
│                                     │
│  - 不管理後端/前端進程                │
│  - 只提供視窗界面                    │
│  - 管理設定檔                        │
└─────────────────┬───────────────────┘
                  │
                  │ HTTP 連接
                  ↓
┌─────────────────────────────────────┐
│         ./start.sh                  │
│                                     │
│  ┌──────────────┐  ┌──────────────┐│
│  │   Backend    │  │   Frontend   ││
│  │ (server.js)  │  │  (Vite dev)  ││
│  │  Port 3000   │←─│  Port 5173   ││
│  └──────────────┘  └──────────────┘│
└─────────────────────────────────────┘
```

**設計理念:**
- **簡單**: 不需要複雜的進程管理
- **穩定**: Electron 崩潰不影響服務
- **靈活**: 服務可獨立運行,不依賴 Electron

## 📝 開發狀態

- ✅ Phase 1: Electron 基礎架構
- ✅ Phase 2: 簡化為視窗 + 設定管理
- ⏳ Phase 3: 改進設定 UI (未來)
- ⏳ Phase 4: 圖示與視覺資源
- ⏳ Phase 5: 打包成 .dmg
- ⏳ Phase 6: 測試與優化
