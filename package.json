{
  "name": "claude-punk",
  "version": "1.0.0",
  "private": true,
  "description": "A cyberpunk pixel-art bar game that turns AI coding agents into bar patrons",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "npx concurrently -n back,front -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
